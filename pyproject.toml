[project]
name = "inventory-system"
version = "1.0.0"
description = "A modular command-line inventory management system."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
